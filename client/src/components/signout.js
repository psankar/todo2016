import React, {Component} from 'react';

class SignOut extends Component {
    render() {
        return (
            <button>
                Sign Out
            </button>
        );
    }
}

export default SignOut;